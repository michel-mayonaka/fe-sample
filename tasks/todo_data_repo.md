# TODO — データ/リポジトリ (2025-09-27)

- `internal/repo`: ClassCaps/Items の Repo 追加、`UserRepo.Reload()` 実装。
- `App.ReloadAll()` に Weapons/User/Images の一括再読込＋UI再注入を集約。
- `internal/ui/core/load.go` の画像パス直書きを `config` へ（または `embed` 化）。

