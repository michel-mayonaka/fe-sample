// Package geom は、シーンやUIに依存しない幾何ユーティリティを提供します。
//
// 目的:
// - ドメインに近い純粋ロジックを `pkg/game` 配下へ集約し、再利用性とテスト容易性を高める。
// - `helper` 的な曖昧な命名を避け、役割を明確化する（docs/KNOWLEDGE/engineering/naming.md に準拠）。
package geom
