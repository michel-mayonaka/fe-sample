# [task] Provider インタフェースの定義（抽象の確定）

- ステータス: 完了
- 目的: `usecase` が具体実装に依存しないことを確認し、既存 `internal/repo` インタフェース群を Port として明確化する。

## 入力
- タスク01の調査結果（参照元・用途・必要機能）。

## スコープ
- 代表的なポートの確認（`UserRepo`, `InventoryRepo`, `WeaponsRepo`）。
- 必要最小のメソッドセットの再確認（保存/取得/一覧 等）。
- 命名・可視性の整合（docs/NAMING.md に準拠）。

## 非スコープ
- 具体実装の作成（タスク04で実施）。

## 手順
- 既存コードから必要機能を抽出（読み取り専用）。
- `internal/repo` の各 IF を見直し、循環しないことを確認。
- 呼称・責務のレビュー観点を列挙（データ境界、トランザクション、エラー方針）。

## DoD（完了条件）
- インタフェース一覧（名前・意図・シグネチャ案）が本ファイルにまとまっている。
- 循環依存を生まない配置/名前空間の方針が明記されている。
- 合意後、実装タスク（04）へ渡せる状態。

## メモ
- 依存方向: `usecase -> repo(if) <- repo/json, repo/sqlite`
